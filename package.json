{
  "name": "inytel-presence",
  "engines": {
    "node": ">=18",
    "pnpm": ">=10"
  },
  "scripts": {
    "build": "cd frontend && pnpm install && pnpm run build",
    "start": "node backend/server.js"
  }
}
